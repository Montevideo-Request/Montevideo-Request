import { AdministratorListFilterPipe } from './administrator-list-filter.pipe';

describe('AdministratorListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdministratorListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
