import { TopicsListFilterPipe } from './topics-list-filter.pipe';

describe('TopicsListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicsListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
