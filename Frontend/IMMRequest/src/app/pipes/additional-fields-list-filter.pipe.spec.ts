import { AdditionalFieldsListFilterPipe } from './additional-fields-list-filter.pipe';

describe('AdditionalFieldsListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdditionalFieldsListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
