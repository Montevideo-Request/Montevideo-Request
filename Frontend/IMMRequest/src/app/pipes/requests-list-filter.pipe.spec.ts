import { RequestsListFilterPipe } from './requests-list-filter.pipe';

describe('RequestsListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RequestsListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
