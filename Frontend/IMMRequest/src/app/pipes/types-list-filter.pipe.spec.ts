import { TypesListFilterPipe } from './types-list-filter.pipe';

describe('TypesListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TypesListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
